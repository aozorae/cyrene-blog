# 开发导览

## 新成员最重要的一点

**博客和后台在同一仓库，但必须始终保持两个独立部署单元。** 根目录是可独立构建的 Astro 静态博客，`admin/` 是独立的 Cloudflare Worker 管理后台。后台不能成为博客运行时依赖。

同时牢记：**GitHub 仓库是已发布文章、动态和配置的唯一真源；D1 不是内容数据库，只保存后台设置、草稿、登录限流和审计记录。** 后台的“发布”本质是向 GitHub 创建提交，再由静态托管平台自动构建博客。

## 项目结构

### 管理后台（重点）

- `admin/src/index.ts`：Worker 入口，区分 `/api/*` 请求与静态管理界面。
- `admin/src/router.ts`、`admin/src/routes-*.ts`：认证、初始化、仪表盘、内容、配置和草稿 API。
- `admin/src/*-service.ts`、`github.ts`、`draft-submit.ts`：业务处理、GitHub API、冲突检测和批量提交。
- `admin/public/`：后台管理界面及 Vditor 等静态资源。
- `admin/migrations/`：D1 表结构与迁移。
- `admin/scripts/`：部署配置准备和前端依赖同步脚本。
- `.github/workflows/deploy-admin.yml`：后台唯一正式部署入口，负责 D1 初始化、迁移、Worker 部署和 secrets 注入。

后台请求链路通常是：`index.ts` -> `router.ts` -> `routes-*.ts` -> 业务服务 -> GitHub 或 D1。新增功能时保持这层职责，不要把路由、数据访问和业务规则塞进同一个文件。

### 博客前端

- `src/pages/`：Astro 页面与少量静态 API。
- `src/components/`、`src/layouts/`：Astro/Svelte 组件和页面布局。
- `src/content/`：文章、动态及独立页面内容。
- `src/config/`：博客配置，也是后台配置编辑最终提交到 GitHub 的位置。
- `src/styles/`、`src/assets/`、`public/`：样式和静态资源。
- `scripts/`：构建期图片、字体等处理脚本。
- `dist/`：博客构建产物，当前主要部署到 Vercel。

## 开发与验证

根目录使用 `pnpm`：

```bash
pnpm dev
pnpm check
pnpm type-check
pnpm build
```

后台在 `admin/` 中独立安装和检查：

```bash
pnpm dev
pnpm check
```

后台生产部署只走 GitHub Actions；敏感信息只放在 GitHub Secrets 或 Wrangler secrets 中，禁止写入源码和配置文件。涉及删除云资源、清空或覆盖生产数据、重置数据库时，必须先确认。

更完整的架构边界与数据流见 `docs/admin-worker-architecture.md`。
